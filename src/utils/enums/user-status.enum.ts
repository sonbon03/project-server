export enum Status {
  PENDING = 'pending',
  CANCEL = 'cancel',
  ACTIVE = 'active',
}

export enum StatusAttibute {
  HAVE = 'have',
  NOT = 'not',
}

export enum StatusPayment {
  PENDING = 'pending',
  PAID = 'paid',
  UNPAID = 'unpaid',
}
